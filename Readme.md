# Полное руководство по проекту Mortgage Admin Panel

## 📋 О проекте

Прототип интерфейса для управления банками-партнерами и ипотечными программами. Приложение позволяет добавлять новые банки, редактировать условия по ипотечным программам (ставки, взносы, сроки), а также управлять видимостью банков в публичном калькуляторе.

### Функциональность

- **Список банков**: Просмотр всех добавленных банков с логотипами и статусами
- **Фильтрация**: Быстрый поиск по конкретному банку, статусу активности или наличию программ
- **Редактирование**: Управление общей информацией о банке (название, логотип, описание)
- **Ипотечные программы**: Добавление и настройка программ (Семейная, IT, Стандартная и др.) с указанием ставок, ПВ и сроков
- **Автоставки**: Возможность переключения программ в режим автоматического обновления ставок (симуляция)
- **Массовые действия**: Переключатель "Применить ко всем проектам"

### Технологический стек

- **React 18**
- **TypeScript**
- **Vite**
- **Tailwind CSS**
- **Lucide React** (иконки)

---

## 🖥️ Локальная разработка

### Требования

- **Node.js**: версия 16 или выше (проверить: `node --version`)
- **npm**: версия 7 или выше (обычно устанавливается вместе с Node.js)

### Установка и запуск

1. **Клонируйте репозиторий** (если еще не склонирован):
   ```bash
   git clone <repository-url>
   cd mortgage-admin-panel
   ```

2. **Установите зависимости**:
   ```bash
   npm install
   ```

3. **Запустите режим разработки**:
   ```bash
   npm run dev
   ```
   
   Приложение будет доступно по адресу `http://localhost:5173` (или другой порт, если 5173 занят).
   
   После запуска вы увидите сообщение:
   ```
   VITE v5.x.x  ready in xxx ms
   
   ➜  Local:   http://localhost:5173/
   ➜  Network: use --host to expose
   ```

4. **Сборка для продакшена**:
   ```bash
   npm run build
   ```
   
   Собранные файлы будут находиться в папке `dist/`.

5. **Предпросмотр продакшен-сборки**:
   ```bash
   npm run preview
   ```
   
   Это запустит локальный сервер для проверки собранного приложения.

### Полезные команды

- `npm run dev` - Запуск в режиме разработки
- `npm run build` - Сборка для продакшена
- `npm run preview` - Предпросмотр продакшен-сборки

---

## 🐛 Решение проблем при локальном запуске

### Проблема: "localhost отказано в подключении"

**Решение:**
1. Убедитесь, что команда `npm run dev` выполняется и сервер запущен
2. Проверьте, что порт 5173 не занят другим приложением
3. Попробуйте открыть другой порт (Vite автоматически выберет свободный)
4. Проверьте, что в консоли нет ошибок при запуске

### Проблема: Ошибки компиляции

**Решение:**
1. Убедитесь, что все зависимости установлены: `npm install`
2. Проверьте версию Node.js (должна быть 16+): `node --version`
3. Удалите `node_modules` и `package-lock.json`, затем выполните `npm install` заново:
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

### Проблема: Порт занят

**Решение:**
Vite автоматически выберет свободный порт. Или укажите другой порт в `vite.config.ts`:
```typescript
server: {
  port: 3000, // или другой свободный порт
}
```

### Проверка работоспособности

После успешного запуска вы должны увидеть:
- В консоли: сообщение о том, что сервер запущен
- В браузере: интерфейс административной панели для управления банками

---

## 🌐 Развертывание на GitHub Pages

### Вариант 1: Автоматический деплой через GitHub Actions (рекомендуется)

Этот вариант использует GitHub Actions для автоматического деплоя при каждом push в ветку `main`.

#### Шаг 1: Подготовка репозитория

1. **Создайте репозиторий на GitHub**:
   - Перейдите на [GitHub](https://github.com)
   - Нажмите "New repository"
   - Введите название (например, `mortgage-admin-panel`)
   - Выберите Public или Private
   - **НЕ** добавляйте README, .gitignore или лицензию (если они уже есть в проекте)
   - Нажмите "Create repository"

2. **Инициализируйте Git и отправьте код**:
   ```bash
   # Если Git еще не инициализирован
   git init
   
   # Добавьте все файлы
   git add .
   
   # Создайте первый коммит
   git commit -m "Initial commit"
   
   # Переименуйте ветку в main (если нужно)
   git branch -M main
   
   # Добавьте remote репозиторий (замените ВАШ_USERNAME на ваш GitHub username)
   git remote add origin https://github.com/ВАШ_USERNAME/mortgage-admin-panel.git
   
   # Отправьте код в репозиторий
   git push -u origin main
   ```

#### Шаг 2: Настройка GitHub Pages

1. **Откройте настройки репозитория**:
   - Перейдите в ваш репозиторий на GitHub
   - Нажмите на вкладку **Settings** (в верхнем меню)

2. **Настройте GitHub Pages**:
   - В левом меню найдите раздел **Pages** (в секции "Code and automation")
   - В разделе **Source** выберите **GitHub Actions** (не "Deploy from a branch")
   - Сохраните изменения (если нужно)

#### Шаг 3: Проверка GitHub Actions workflow

Файл `.github/workflows/deploy.yml` уже должен быть в проекте. Проверьте его содержимое:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

**Если файла нет**, создайте его:

1. Создайте папку `.github/workflows` в корне проекта:
   ```bash
   mkdir -p .github/workflows
   ```

2. Создайте файл `deploy.yml` в этой папке с содержимым выше

3. Добавьте и закоммитьте файл:
   ```bash
   git add .github/workflows/deploy.yml
   git commit -m "Add GitHub Actions workflow"
   git push
   ```

#### Шаг 4: Запуск деплоя

После настройки GitHub Pages и отправки кода в репозиторий:

1. **Автоматический запуск**: GitHub Actions автоматически запустится при push в ветку `main`

2. **Ручной запуск** (опционально):
   - Перейдите в репозиторий на GitHub
   - Откройте вкладку **Actions**
   - В левом меню выберите workflow **"Deploy to GitHub Pages"**
   - Нажмите **"Run workflow"** справа
   - Выберите ветку `main` и нажмите **"Run workflow"**

#### Шаг 5: Мониторинг деплоя

1. **Проверьте статус деплоя**:
   - Перейдите во вкладку **Actions** в репозитории
   - Вы увидите запущенный workflow "Deploy to GitHub Pages"
   - Нажмите на него, чтобы увидеть детали

2. **Проверьте логи**:
   - В workflow вы увидите два job: `build` и `deploy`
   - Нажмите на каждый, чтобы увидеть логи выполнения
   - Если есть ошибки, они будут отображены в логах

3. **Дождитесь завершения**:
   - Обычно деплой занимает 1-3 минуты
   - После успешного деплоя вы увидите зеленую галочку ✅

#### Шаг 6: Доступ к приложению

После успешного деплоя приложение будет доступно по адресу:
```
https://ВАШ_USERNAME.github.io/mortgage-admin-panel/
```

> **Примечание**: 
> - После первого деплоя сайт может появиться через несколько минут (до 10 минут)
> - Если сайт не открывается, проверьте вкладку **Actions** на наличие ошибок
> - Убедитесь, что в настройках Pages выбран источник **GitHub Actions**

#### Обновление деплоя

После настройки, каждый раз когда вы делаете `git push` в ветку `main`, GitHub Actions автоматически:
1. Соберет проект
2. Задеплоит его на GitHub Pages
3. Обновит сайт

Просто работайте как обычно:
```bash
git add .
git commit -m "Ваши изменения"
git push
```

---

### Вариант 2: Ручной деплой (альтернатива)

Если по какой-то причине вы не хотите использовать GitHub Actions, можно задеплоить вручную:

1. **Соберите проект**:
   ```bash
   npm run build
   ```

2. **Настройте репозиторий для GitHub Pages**:
   ```bash
   # Перейдите в папку dist
   cd dist
   
   # Инициализируйте git (если еще не инициализирован)
   git init
   git add .
   git commit -m "Deploy to GitHub Pages"
   
   # Создайте ветку gh-pages
   git branch -M gh-pages
   
   # Добавьте remote (замените на ваш репозиторий)
   git remote add origin https://github.com/ВАШ_USERNAME/mortgage-admin-panel.git
   
   # Запушьте в ветку gh-pages
   git push -u origin gh-pages
   ```

3. **Включите GitHub Pages**:
   - Перейдите в Settings → Pages вашего репозитория
   - В разделе "Source" выберите ветку `gh-pages` и папку `/ (root)`
   - Сохраните изменения

4. **Приложение будет доступно** по адресу:
   `https://ВАШ_USERNAME.github.io/mortgage-admin-panel/`

   > **Примечание**: После первого деплоя может потребоваться несколько минут для активации сайта.

5. **Для обновления**: Повторите шаги 1-2 каждый раз, когда нужно обновить сайт.

---

## 📁 Структура проекта

```
mortgage-admin-panel/
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions workflow для автоматического деплоя
├── components/                 # React компоненты
│   ├── ui/                     # UI компоненты (Button, Switch)
│   │   ├── Button.tsx
│   │   └── Switch.tsx
│   ├── BankFormModal.tsx       # Модальное окно для редактирования банка
│   ├── BankTable.tsx           # Таблица со списком банков
│   ├── ConfirmationModal.tsx   # Модальное окно подтверждения
│   └── Toast.tsx               # Компонент уведомлений
├── services/                   # Сервисы и утилиты
│   └── mockData.ts            # Моковые данные для разработки
├── src/                        # Исходный код приложения
│   ├── App.tsx                # Главный компонент
│   ├── index.tsx              # Точка входа
│   ├── index.css              # Глобальные стили
│   └── types.ts               # TypeScript типы
├── index.html                  # HTML шаблон
├── package.json                # Зависимости и скрипты
├── vite.config.ts             # Конфигурация Vite
├── tsconfig.json               # Конфигурация TypeScript
├── tsconfig.node.json          # Конфигурация TypeScript для Node.js
├── tailwind.config.js          # Конфигурация Tailwind CSS
├── postcss.config.js           # Конфигурация PostCSS
└── README.md                   # Документация
```

---

## 🔧 Конфигурация

### Vite

Файл `vite.config.ts` настроен для работы на GitHub Pages:
- `base: './'` - относительные пути для корректной работы на GitHub Pages
- `server` - настройки dev-сервера (порт, host)

### TypeScript

- Строгий режим включен
- Поддержка React JSX
- Разрешение модулей через bundler

### Tailwind CSS

- Настроен для сканирования файлов в `src/`, `components/` и `index.html`
- Кастомные цвета и анимации определены в конфигурации

---

## 📝 Примечания

- Для автоматического деплоя используется GitHub Actions (файл `.github/workflows/deploy.yml`)
- Конфигурация Vite уже настроена для работы на GitHub Pages (`base: './'`)
- После первого деплоя сайт может появиться через несколько минут
- GitHub Actions требует, чтобы в настройках репозитория был выбран источник "GitHub Actions" для Pages
- При каждом push в ветку `main` автоматически запускается деплой

---

## ❓ Часто задаваемые вопросы

### Как проверить, что GitHub Actions работает?

1. Перейдите во вкладку **Actions** в вашем репозитории
2. Вы должны увидеть workflow "Deploy to GitHub Pages"
3. Если workflow не запускается автоматически, проверьте:
   - Что файл `.github/workflows/deploy.yml` существует
   - Что вы делаете push в ветку `main`
   - Что в настройках Pages выбран источник "GitHub Actions"

### Почему сайт не открывается после деплоя?

1. Проверьте вкладку **Actions** на наличие ошибок
2. Убедитесь, что деплой завершился успешно (зеленая галочка)
3. Подождите несколько минут (первый деплой может занять до 10 минут)
4. Проверьте URL: `https://ВАШ_USERNAME.github.io/mortgage-admin-panel/`
5. Убедитесь, что в настройках Pages выбран источник "GitHub Actions"

### Как изменить название репозитория?

Если вы изменили название репозитория на GitHub, обновите URL в настройках Pages. URL сайта изменится соответственно.

### Можно ли использовать другой порт для локальной разработки?

Да, измените настройки в `vite.config.ts`:
```typescript
server: {
  port: 3000, // ваш порт
}
```

---

## 🆘 Получение помощи

Если у вас возникли проблемы:

1. Проверьте логи в консоли при запуске `npm run dev`
2. Проверьте вкладку **Actions** на GitHub на наличие ошибок
3. Убедитесь, что все зависимости установлены: `npm install`
4. Проверьте версию Node.js: `node --version` (должна быть 16+)

